name: One-time Main check-in text safety

on:
  push:
    branches: [main]
    paths:
      - 'scripts/one-time-main-checkin-text-safety.py'
      - '.github/workflows/one-time-main-checkin-text-safety.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Escape reused free text in Main check-in surfaces
        run: python scripts/one-time-main-checkin-text-safety.py
      - name: Commit exact fix and remove helpers
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'ChatGPT (OpenAI)'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html tests/main-user-text-safety-smoke.js
          git rm scripts/one-time-main-checkin-text-safety.py .github/workflows/one-time-main-checkin-text-safety.yml
          git commit -m 'ChatGPT: render Main check-in free text safely'
          git push origin HEAD:main
