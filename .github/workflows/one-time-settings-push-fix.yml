name: One-time Settings real push fix

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one-time-settings-push-fix.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Patch Settings
        run: python3 tools/one-time-settings-push-fix.py
      - name: Commit exact fix and remove helpers
        run: |
          set -euo pipefail
          rm .github/workflows/one-time-settings-push-fix.yml tools/one-time-settings-push-fix.py
          git config user.name "ChatGPT (OpenAI)"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add settings.html .github/workflows/one-time-settings-push-fix.yml tools/one-time-settings-push-fix.py
          git commit -m "ChatGPT: connect Settings to real push subscription"
          git push origin HEAD:main
