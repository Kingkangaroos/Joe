name: Venture Lab prototype workbench integration

on:
  push:
    branches:
      - chatgpt/venture-lab-prototype-workbench

permissions:
  contents: write

jobs:
  integrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: chatgpt/venture-lab-prototype-workbench
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: '22'
      - name: Apply Venture Lab patch
        run: python3 scripts/venture-lab-workbench-patch.py
      - name: Run all smoke tests
        shell: bash
        run: |
          set -euo pipefail
          for test in tests/*-smoke.js; do
            echo "::group::$test"
            node "$test"
            echo "::endgroup::"
          done
      - name: Remove one-shot integration files
        run: |
          rm scripts/venture-lab-workbench-patch.py
          rm .github/workflows/venture-lab-workbench-integration.yml
      - name: Commit proven integration
        run: |
          git config user.name "ChatGPT (OpenAI)"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "ventures: move Website prototypes into Venture Lab"
          git push origin HEAD:chatgpt/venture-lab-prototype-workbench
