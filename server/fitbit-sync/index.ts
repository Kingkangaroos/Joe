// Deployed als v6 (v9.17): Google Health API v4 — OAuth (?auth=1 / ?code=), tokens in google_health_tokens,
// daily pull steps/AZM/sleep/RHR/weight → health_fitbit (60 dagen accumulerend). Gekalibreerd schema: zie GAMENFY-MASTER.md §7.
// Secrets alleen in de gedeployde functie.
